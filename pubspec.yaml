name: dart_vcd
description: Read and write VCD (Value Change Dump) files
version: 1.0.3
repository: https://github.com/definev/dart_vcd

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  collection: ^1.17.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
